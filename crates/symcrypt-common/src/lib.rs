//! `symcrypt-common` — terminal glue shared by the CLI and TUI front-ends:
//! path-or-stdin I/O, clobber checks, temp-file finalization, best-effort
//! remove, password-source resolution, and exit-code mapping.
